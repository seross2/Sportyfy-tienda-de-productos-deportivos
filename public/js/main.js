// Este archivo ha sido reemplazado por app.js y se mantiene para evitar errores 404
// si todavía está referenciado en el HTML. La lógica principal ahora reside en
// las clases GlobalApp y ProductApp en app.js.